import { render } from '../test-utils'
import Root from 'routes/Root'
import { MemoryRouter } from 'react-router-dom';

describe('18+ Access', () => {
    test.only('when the app is first opened, the 18+ notice is presented', () => {

        // render(
        //     <MemoryRouter>
        //         <Root />
        //     </MemoryRouter>
        // );

        throw new Error('to do');
    })
    // test('the 18+ notice can be viewed from the main screen', () => {
    //     throw new Error('to do');

    // })
    // test('if 18+ is accepted, then the user can use the app', () => {
    //     throw new Error('to do');

    // })
    // test('declining 18+ redirects the user to the 18+ only page and the', () => {
    //     throw new Error('to do');

    // })
    // test('clicking outside the modal declines 18+', () => {
    //     throw new Error('to do');

    // })
    // test('if 18+ is declined, then the user cannot use the app until they accept', () => {
    //     throw new Error('to do');

    // })
})

// describe('GDPR', () => {
//     test('when the app is first opened, the gdpr data policy can be viewed', () => {
//         throw new Error('to do');
        
//     })
//     test('the gdpr policy can be viewed anytime', () => {
//         throw new Error('to do');

//     })
//     test('the user can clear their device data', () => {
//         throw new Error('to do');

//     })
// })
